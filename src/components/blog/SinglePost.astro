---
import { Icon } from 'astro-icon/components';

import PostTags from '~/components/blog/Tags.astro';

import { getPermalink } from '~/utils/permalinks';
import { getFormattedDate } from '~/utils/utils';

import type { Post } from '~/types';

export interface Props {
  post: Post;
  url: string | URL;
}

const { post } = Astro.props;
---

<section class="py-8 sm:py-16 lg:py-20 mx-auto">
  <article>
    <header>
      <div class="flex justify-between flex-col sm:flex-row max-w-3xl mx-auto mt-0 mb-2 px-4 sm:px-6 sm:items-center">
        <p>
          <Icon name="tabler:clock" class="w-4 h-4 inline-block -mt-0.5 dark:text-gray-400" />
          <time datetime={String(post.publishDate)} class="inline-block">{getFormattedDate(post.publishDate)}</time>
          {
            post.author && (
              <>
                {' '}
                · <Icon name="tabler:user" class="w-4 h-4 inline-block -mt-0.5 dark:text-gray-400" />
                <span class="inline-block">{post.author}</span>
              </>
            )
          }
          {
            post.category && (
              <>
                {' '}
                ·{' '}
                <a class="hover:underline inline-block" href={getPermalink(post.category.slug, 'category')}>
                  {post.category.title}
                </a>
              </>
            )
          }
          {
            post.readingTime && (
              <>
                &nbsp;· <span>{post.readingTime}</span> min read
              </>
            )
          }
        </p>
      </div>

      <h1
        class="px-4 sm:px-6 max-w-3xl mx-auto text-4xl md:text-5xl font-bold leading-tighter tracking-tighter font-heading"
      >
        {post.title}
      </h1>
      <p
        class="max-w-3xl mx-auto mt-4 mb-8 px-4 sm:px-6 text-xl md:text-2xl text-muted dark:text-slate-400 text-justify"
      >
        {post.excerpt}
      </p>

      {/* Hero: real product photo when provided, otherwise brand panel */}
      <div class="max-w-full lg:max-w-[900px] mx-auto mb-6 px-4 sm:px-0">
        {
          typeof post.image === 'string' && post.image ? (
            <div class="relative aspect-[21/9] overflow-hidden rounded-xl border border-stone-200/80 bg-soft">
              <img
                src={post.image}
                alt={post.title}
                class="absolute inset-0 h-full w-full object-cover"
                loading="eager"
                fetchpriority="high"
              />
              {post.category?.title && (
                <span class="absolute bottom-4 left-4 text-[10px] font-semibold uppercase tracking-[0.14em] bg-elevated/95 border border-stone-200/80 px-2.5 py-1 text-heading">
                  {post.category.title}
                </span>
              )}
            </div>
          ) : (
            <div class="relative aspect-[21/9] overflow-hidden rounded-xl border border-stone-200/80 bg-soft">
              <div
                class="absolute inset-0 bg-gradient-to-br from-primary/15 via-transparent to-accent/10"
                aria-hidden="true"
              />
              <div class="absolute inset-0 flex items-center justify-center">
                <span class="font-heading text-3xl md:text-4xl font-semibold text-primary/35 tracking-tight">
                  GutSource
                </span>
              </div>
              {post.category?.title && (
                <span class="absolute bottom-4 left-4 text-[10px] font-semibold uppercase tracking-[0.14em] bg-elevated/95 border border-stone-200/80 px-2.5 py-1 text-heading">
                  {post.category.title}
                </span>
              )}
            </div>
          )
        }
      </div>
    </header>
    <div
      class="mx-auto px-6 sm:px-6 max-w-3xl prose prose-md lg:prose-xl dark:prose-invert dark:prose-headings:text-slate-300 prose-headings:font-heading prose-headings:leading-tighter prose-headings:tracking-tighter prose-headings:font-bold prose-a:text-primary dark:prose-a:text-blue-400 prose-img:rounded-md prose-img:shadow-lg mt-8 prose-headings:scroll-mt-[80px] prose-li:my-0"
    >
      <slot />
    </div>
    <div class="mx-auto px-6 sm:px-6 max-w-3xl mt-8">
      <PostTags tags={post.tags} />
    </div>
  </article>
</section>
